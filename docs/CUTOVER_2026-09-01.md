# 本机服务切换记录

完成时间：2026-09-01 00:19（Asia/Shanghai）。用户批准短暂停写、备份并切换新版；本次没有重新发布公网。

## 结果

- 本机地址：<http://127.0.0.1:4173/>，也支持 <http://localhost:4173/>。
- 原正式数据库、附件和账号保留；没有执行初始化、演示播种、密码重置或数据库恢复。
- 新旧 API 兼容；新版 `/api/v1/health` 返回成功与 `apiVersion: v1`。
- 旧服务及其临时隧道已退出。新版不创建隧道，网关/API 分别仅监听 `127.0.0.1:4173/4174`。
- 数据目录仍为 `.data/selfhost/`。服务需要当前电脑及运行进程保持开启；未配置系统开机自启。

## 备份与回退材料

以下均为项目根目录下、Git 忽略的本机路径：

- 一致性备份：`.data/selfhost/backups/2026-08-31T16-18-34-394Z-01806e58/`。
- 旧构建核验副本：`.selfhost/cutover-20260901-A7G3Ea/old-artifacts/`。
- 原活动构建：`.selfhost/cutover-20260901-A7G3Ea/previous-active/`。
- 原启动记录及主管源码副本：`.selfhost/cutover-20260901-A7G3Ea/`。
- 只读数据比较脚本：该目录下 `verify-data.mjs`，参数为备份目录、当前数据目录。

备份包含数据库快照及其引用的 1 个附件，独立校验数据库和附件 SHA-256；不是整个附件目录的全量归档。原附件目录完全保留。备份含业务记录及密码哈希，应受控保管，不上传公开仓库。

必要时先暂停提交、停止并确认本项目所有服务已退出，将新构建移至独立保留目录，再恢复旧 `web/api/gateway` 构建，使用同一正式数据库启动本机服务。只回退构建，不自动恢复数据库；用户恢复录入后的新记录必须保留。数据出现异常时应停写检查，不能静默覆盖原库。

## 验证证据

- 切换前重新构建，9 项正式模式隔离测试通过，覆盖认证、CSRF、区县权限、v1、附件、备份恢复与重启。
- 旧构建复制后逐文件比较一致；新版安装后与隔离构建逐文件一致。
- 停止后确认旧主管、API、网关和隧道退出；服务端口和数据库无占用，再执行备份。
- 启动前后分别比较备份与原库：12 张业务/配置/账户表行数和内容摘要、应用 Schema 一致；完整性及外键检查通过。
- 会话和登录限流两张运行态表不纳入逐行一致性比较；没有读取或展示用户口令、记录内容。
- 1 个引用附件的字节数及 SHA-256 一致。
- 旧版健康接口、新版健康接口：200；未登录业务接口：401；绕过网关直连 API：403；尝试下载数据库：404。
- 首页 HTML 及 10 个 JS/CSS 资源与新构建摘要相同；首页 `no-store` 和 CSP 响应头存在。
- 主管及子进程在启动命令返回后仍运行，实际打开的仍是原正式数据库。

未进行真实账号登录、业务写入或浏览器/手机交互验收。Docker 运行、公网 HTTPS、财务请求幂等和更完整的安全验收仍是后续工作。

Route: Luna -> Sol -> Luna。触发：正式服务可用性与数据回退风险；Sol 只读审核切换顺序，执行端完成备份、升级和只读验收。
