# 公网入口恢复记录

2026-09-01 00:43（Asia/Shanghai），使用已部署的正式自托管构建恢复 Cloudflare 临时入口：

<https://handbags-forgot-episode-structural.trycloudflare.com>

用户已明确授权：通过 Cloudflare 临时域名访问系统，网页、登录后读取的业务数据及附件经 Cloudflare 中转；数据库保留本机，原正式账号及权限不变。不涉及 Sites 演示站点发布。

## 验证

- 先用只返回维护标记的隔离服务验证 HTTP/2 隧道注册及 HTTPS 往返，随后关闭测试隧道。
- 通过原主管启动正式隧道，准确的新 HTTPS Origin 同时传入网关/API；没有修改系统代理、DNS、防火墙或 TLS 校验。
- 实际公网 HTTPS 首页 200，HTML 及两个入口 JS/CSS 摘要与当前构建一致。
- 公网 v1 健康接口 200；新旧匿名 bootstrap 401；匿名附件请求 401；数据库下载路径 404；匿名会话为空。
- 公网 CSP/HSTS 响应头存在。未用真实账号进行登录或业务写入测试。
- 网关/API 仍仅监听 127.0.0.1:4173/4174；主管及 API、网关、cloudflared 三个子进程持续运行。
- 12 张持久表及 Schema 与最近备份一致，1 个引用附件摘要一致，完整性和外键检查通过。
- 公网请求由本机经实际 HTTPS 域名验证；独立网页抓取工具拒绝打开临时域名，因此没有宣称异地网络或手机端已验收。

## 运维

最近备份：`.data/selfhost/backups/2026-08-31T16-31-37-826Z-6b1ac709/`。原库、附件和账号未重置，也没有恢复旧数据覆盖现有记录。

实际入口与进程状态以 `.selfhost/runtime.json` 为准。临时地址可能在重启后改变；电脑须保持开机、联网，运行进程不能退出。未设置系统开机自启或承诺持续在线。

Mac 上短启动参数可能导致 process.title 被截断，使主管识别失败；本次通过原保留终端正常停止，并改用脚本绝对路径启动，已核实完整主管标记可识别。没有放宽进程归属校验。

Route: Luna -> Sol -> Luna，沿用同一已审核公网恢复方案；执行端完成隧道启动、HTTPS、权限及数据一致性校验。
